uuid: 00e085da-aea7-41ff-9d23-53121194f686
langcode: en
status: true
dependencies:
  config:
    - node.type.recurring_ride
    - node.type.ride
  module:
    - content_moderation
id: rides
label: Rides
type: content_moderation
type_settings:
  states:
    archived:
      label: Archived
      weight: 2
      published: false
      default_revision: false
    draft:
      label: Draft
      weight: -3
      published: false
      default_revision: false
    finalize_draft:
      label: 'Ready for review'
      weight: -2
      published: false
      default_revision: false
    publish_and_notify:
      label: 'Published (notify author)'
      weight: 0
      published: true
      default_revision: true
    published:
      label: Published
      weight: 1
      published: true
      default_revision: true
  transitions:
    archive:
      label: Archive
      from:
        - draft
        - publish_and_notify
        - published
      to: archived
      weight: 2
    create_new_draft:
      label: 'Create New Draft'
      from:
        - archived
        - draft
        - finalize_draft
        - publish_and_notify
        - published
      to: draft
      weight: -3
    finalize_draft:
      label: Review
      from:
        - archived
        - draft
      to: finalize_draft
      weight: -2
    publish:
      label: Publish
      from:
        - archived
        - draft
        - finalize_draft
        - publish_and_notify
        - published
      to: published
      weight: 1
    publish_notify:
      label: 'Publish & notify'
      from:
        - finalize_draft
      to: publish_and_notify
      weight: 0
  entity_types:
    node:
      - recurring_ride
      - ride
  default_moderation_state: draft
