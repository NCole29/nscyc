langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_ride_leader
    - node.type.recurring_ride
  enforced:
    module:
      - club_ride
id: node.recurring_ride.field_ride_leader
field_name: field_ride_leader
entity_type: node
bundle: recurring_ride
label: Leader(s)
description: "Enter one name per text box. Begin typing name in field to get a list of ride leaders matching your search string and select from list. Leader must be a club member and designated ride leader."
required: false
translatable: true
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:user'
  handler_settings:
    include_anonymous: true
    filter:
      type: role
      role:
        ride_leader: ride_leader
        board_member: '0'
        administrator: '0'
    target_bundles: null
    sort:
      field: name
      direction: ASC
    auto_create: false
field_type: entity_reference
