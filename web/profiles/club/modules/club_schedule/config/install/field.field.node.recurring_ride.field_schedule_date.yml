langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_schedule_date
    - node.type.recurring_ride
  enforced:
    module:
      - club_schedule
id: node.recurring_ride.field_schedule_date
field_name: field_schedule_date
entity_type: node
bundle: recurring_ride
label: 'Schedule date'
description: 'Field is hidden. If page title is "Wednesday Morning Rotating Ride", this field is  filled automatically (see club_ride.module hook_presave) by matching the ride date with the "Schedule of ride dates" (custom entity) to provide a link with the schedule. '
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: views
  handler_settings:
    view:
      view_name: schedule_ride_dates
      display_name: wednesday_list
      arguments: {  }
field_type: entity_reference
